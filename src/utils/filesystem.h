#ifndef FILESYSTEM_H
#define FILESYSTEM_H

#include <list>
#include <string>

// List all files under a directory
std::list<std::string> utils_listDirectoryFiles(const char* dir);


#endif // FILESYSTEM_H
